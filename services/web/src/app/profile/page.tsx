import { ScreenLayout } from "@/components";
import React from "react";

const Profile = () => {
  return (
    <ScreenLayout title="프로필">
      <div>프로필</div>
    </ScreenLayout>
  );
};

export default Profile;
